# AI 项目交接指南

**简体中文** | [English](AI_PROJECT_GUIDE.en.md)

> **用途：** 本文档专门用于让 AI 编程助手快速理解 Game Archive。把本文件连同具体任务交给 AI，可减少它重新猜测产品目标、时间线结构和仓库潜规则的过程。

本文记录的是当前项目约定，不替代各时间线自己的收录规则和研究来源。开始修改前，AI 仍应读取与任务直接相关的代码和数据，不能只凭本文推测实现。

## 一句话理解项目

Game Archive 是一个以纵向时间线整理游戏硬件与长期游戏系列的静态资料库。一级卡片负责快速识别，点击后在同一个连接窗口中逐层展示玩法、机制、影响、剧情、平台与时间线专属资料。项目既服务普通玩家快速查阅，也服务游戏策划与研究者追踪一个系列为什么分叉、哪些实验被继承、修正或放弃。

## 开始工作前的阅读顺序

1. [`README.md`](README.md)：公开定位、当前收录和用户阅读方式。
2. 本文档：仓库结构、接入流程和不可破坏的规则。
3. [`common/TIMELINE_CARD_ARCHITECTURE.md`](common/TIMELINE_CARD_ARCHITECTURE.md)：所有时间线共用的卡片和布局契约。
4. [`RESEARCH_SOURCES.md`](RESEARCH_SOURCES.md)：日期、平台、内容分析、证据等级和图片来源规则。
5. 目标时间线目录下的 `README.md` 与 `selection-criteria.js`：该时间线真正收什么、不收什么。
6. `AGENTS.md`：本仓库的验证安全要求。

## 当前产品结构

### 资料库层级

- **游戏主机**：硬件时间线。
- **游戏系列**：总览和各系列独立时间线。
- **总览**：用户勾选系列后，把已注册系列的原始一级卡片和原始展开信息按日期合并；默认不选择任何系列。
- **GM 图片管理**：仅供仓库维护者本地使用，不是公开用户功能。

### 当前时间线

- Hardware
- Pokemon
- Final Fantasy
- Dragon Quest
- Xeno Series
- Like a Dragon
- Spike Chunsoft Narrative

游戏系列页签按加入项目的顺序排列，不按字母排序。除非用户明确要求调整，否则新增时间线追加在现有时间线之后。总览筛选器使用相同顺序。

## 技术形态

- 无框架、无构建步骤的静态网站。
- `index.html` 以 `<script>` 顺序加载各时间线数据，数据通过 `window.*` 全局变量提供给 `common/app.js`。
- `common/app.js` 负责共享渲染、交互、总览、详情窗口和 GM 界面。
- `common/styles.css` 负责共享尺寸、间距、排版、主题色和响应式样式。
- `common/language.js` 负责语言选择、URL 参数与本地记忆；`common/localization-en-data.js` 保存机器辅助生成的英文初稿，`common/localization-en-reviewed.js` 保存人工审定、不可被重新生成覆盖的英文文本，`common/localization-en.js` 负责术语优先级、数据转换和动态界面翻译。
- 每条时间线在 `timelines/<TimelineName>/` 内拥有自己的数据、研究文本、里程碑和图片。
- GitHub Pages 直接发布仓库静态文件，所有公开资源路径必须是仓库相对路径。

中文是当前编辑基准。英文模式通过 `?lang=en` 打开，并必须在显眼位置持续显示“未校对”警示，直到用户明确确认已完成人工审校。新增或修改任何用户可见中文时，都必须同步补充英文映射或重新生成英文数据。机器翻译只能生成正文初稿；系列名、作品名、类型、平台、角色、机制和里程碑必须优先采用官方英文资料，并写入共享术语表或人工审定层。不要直接手改 `localization-en-data.js`，因为重新生成会覆盖它。

不要在没有明确重构任务时引入框架、打包器、数据库或服务端依赖。

## 时间线信息层级

### 一级卡片：所有时间线共用规范

一级卡片的外观、尺寸、交互和区域顺序属于全局契约。修改一级卡片时，应通过共享组件让所有时间线同步生效，而不是只改一条时间线。

固定区域顺序：

1. 左上日期；
2. 右上分类标签；
3. 标题；
4. 副标题；
5. 可选的补充关系文本；
6. 左下展开内容提示或平台/型号数量；
7. 右下可选图片区域。

关键规则：

- 日期使用全球范围内最早可确认的公开发售时间。
- 游戏尽量使用完整年月日；硬件只显示证据能够支持的精度。
- 标签右对齐，内容过长时向左延伸，接近日期后以省略号收束，并通过 tooltip 提供全称。
- 标题、副标题被图片压缩时使用省略号和 tooltip，不与图片重叠，也不改变卡片外部尺寸。
- 无图时文本可以使用图片区域；有图时图片在预留区域内右对齐并保持比例。
- 一级卡片的连接线必须真正连接卡片与时间轴，卡片和红点不得重叠。
- 时间线可以压缩长年份空白，但日期先后不能颠倒。

硬件一级卡片显示硬件名、历史地位、关键特点和型号数量；产品线信息放入展开窗口。游戏一级卡片显示原文/英文标题、精简中文副标题、必要的版本关系、平台数量和可选封面或 Logo。

### 展开信息：允许按时间线定制

展开信息统一放在一个与一级卡片相连的窗口中。界面不显示“二级卡片”“三级卡片”“四级卡片”，也不显示“系列解读”“剧情解读”“客观记录”等大框标题，只显示高密度的小字段标题。

游戏系列的标准顺序：

1. 核心体验；
2. 本作变化；
3. 机制逻辑；
4. 系列影响；
5. 有可靠证据时才出现的行业影响；
6. 剧情概要与叙事创新；
7. 首次登陆与后续登陆平台；
8. 时间线专属补充，例如 Pokemon 御三家。

硬件的标准顺序：

1. 产品线身份；
2. 型号与改版；
3. 首发护航游戏与特色游戏。

只修改某条时间线的展开字段时，不要把变化扩散到其他时间线；只有用户明确说“所有时间线”时才修改共享展开结构。

当前 `SHOW_RESEARCH_PROMPTS` 与 `SHOW_DESIGN_DECISION_CHAINS` 均为 `false`。研究线索和设计决策链数据可以保留，但公开界面默认隐藏。

## 研究与写作标准

- 先确定作品在该系列中的收录理由，再写卡片。
- “本作变化”必须有明确比较基线，不能只写“新增某系统”。
- “机制逻辑”要说明玩家输入、系统判定、反馈和下一轮决策形成的闭环。
- “系列影响”必须说明具体规则被哪部后作继承、修正、替代或放弃。
- “行业影响”只在存在可靠证据时显示；没有就隐藏，不用空泛句补齐。
- 剧情概要按作品的叙事权重决定长度，并明确指出可验证的叙事创新。
- 推断必须写成编辑分析，不能冒充官方设计意图。
- 删除“丰富了体验”“为后续奠定基础”“让后续可以继承有效部分”等没有具体对象的车轱辘话。
- 事实、成品可观察规则、多来源分析和证据不足四种情况必须区分。

完整证据和来源规则见 [`RESEARCH_SOURCES.md`](RESEARCH_SOURCES.md)。

## 日期、版本与平台

- 一级卡片日期取全球最早公开发售日，不固定使用日本、北美或欧洲日期。
- 不知道日或月时保留真实精度，不补 `01` 伪装成确定日期。
- 普通移植、廉价版、订阅服务和数字再发行通常写进原作 `later` 平台记录。
- 剧情、结构、系统或制作规模发生实质重构时，才考虑建立新的一级卡片。
- 平台显示采用项目已有缩写：`PlayStation` 可写 `PS`，`PlayStation 4` 写 `PS4`，`Nintendo Switch` 写 `NS`，`Nintendo Switch 2` 写 `NS2`；Xbox 平台通常保留完整世代名。
- 首次登陆与后续登陆在展开窗口中按“年份 + 平台”同行显示，长平台名使用省略号和 tooltip。

## 里程碑

里程碑显示在一级卡片与时间轴之间，不占用卡片内容区。目前有三种：

- `domestic`：在原始市场建立或显著扩大系列认知的节点；
- `global`：形成重要全球化突破的节点；
- `integration`：相关公司、工作室或项目组整合后，能够确认的第一部相关作品。

同一作品可以同时拥有多个标记，必须分别解释。不要只凭销量排名推断里程碑，也不要把发行主体合并误写成两个开发团队共同制作。

## 图片与 GM 管理规则

- 每条时间线的图片放在该时间线目录下，例如 `timelines/LikeADragon/assets/covers/`。
- 硬件图片放在 `timelines/hardware/assets/consoles/`。
- 运行时只读取各时间线的 `timeline-images.js`，不热链 Wiki，也不在页面加载时下载外部图片。
- 图片键格式通常为 `<imageCollectionId>:<release.id>`；硬件使用自己的稳定硬件键。
- 图片数组顺序就是卡片循环和悬浮放大预览的顺序。
- 一张图片只显示该图；多张图片允许循环，并在悬浮时按存储顺序并列预览。
- 悬浮放大只展示图片，不显示图片文件名；预览不能超出视口。
- 日本游戏主动搜图时同时使用中文、英文和日文名称，优先最早日版封面或日文官方素材。
- 通常每个作品保留 1 至 4 张可筛选候选图，避免大量重复素材。
- 不要自动覆盖用户已经通过 GM 选择或导入的图片。
- GM 的添加、替换、排序和删除直接修改仓库内真实图片与清单，不存在“恢复默认图片”。
- 时间线改名时，图片目录、清单全局变量、图片集合 ID、GM 服务映射和公开标签必须一起检查。

GM 服务只在用户明确要操作图片管理工具时通过 `start-gm-server.cmd` 启动。自动验证不得启动后台服务器。

## 新增游戏时间线：完整接入清单

最稳妥的做法是复制结构最接近的现有时间线，再替换内容和全局变量。不要复制后只改页面标题。

### 1. 建立目录

创建 `timelines/<TimelineName>/`，目录名应与时间线名称在语义上保持一致，并使用适合文件系统的写法。标准内容包括：

```text
README.md
selection-criteria.js
releases.js
milestones.js
editorial-reading.js
design-logic.js
series-impact.js
external-impact-research.js
plot-summaries.js
decision-chain.js
timeline-images.js
assets/covers/
```

`decision-chain.js` 当前仍需保持数据结构兼容，即使界面不显示。确实需要覆盖层时可增加类似 `verified-content.js` 的文件，但不要无理由拆分。

### 2. 定义 release 数据

使用稳定、唯一、不会随显示标题变化的 `id`：

```js
window.EXAMPLE_RELEASES = [
  {
    id: "example-1",
    date: "2000.01.01",
    category: "正传",
    tag: "正传 · RPG",
    name: "Example Game",
    chineseName: "示例游戏",
    lineage: "重制：Original Game",
    first: [{ year: 2000, platform: "PS2" }],
    later: [{ year: 2020, platform: "NS / PC" }]
  }
];
```

字段可按时间线需要扩展，但共享渲染依赖的基础字段不能随意改名。

### 3. 写收录规则与研究数据

- `selection-criteria.js` 必须能直接回答“为什么这些作品被选入，哪些作品被排除”。
- 为每个 release ID 提供与共享读取逻辑兼容的编辑解读、机制、影响和剧情记录。
- 行业影响没有证据时不造内容。
- 时间线专属补充通过 definition 的 `supplementFor` 接入，不修改所有系列的共同详情结构。

### 4. 注册图片

- 把候选图复制进该时间线的 `assets/covers/`。
- 在 `timeline-images.js` 中以稳定键注册本地相对路径。
- 在 `common/timeline-image-store.js` 的 fallback 集合中加入该清单全局变量。
- 在 `scripts/local-gm-server.js` 的 `manifestDefinitions` 中登记清单文件、全局变量和图片目录。

### 5. 接入页面

- 在 `index.html` 的系列页签末尾加入按钮和对应 panel，保持创建顺序。
- 在 `index.html` 中按依赖顺序加载该时间线全部脚本，并保证 `timeline-images.js` 在 `common/timeline-image-store.js` 和 `common/app.js` 之前加载。
- 在 `common/app.js` 的元素表和 `gameSeriesDefinitions()` 中注册时间线。提供稳定 `id`、显示 `label`、release 数组、DOM 元素、主题、图片集合 ID、卡片类名、详情类名、颜色、标签函数、收录规则、里程碑和内容映射。
- 新定义会自动进入总览筛选器，但默认不被选中。总览必须复用该时间线原本的卡片与详情，不单独复制一套 UI。
- 在 `common/styles.css` 增加该系列主题变量和必要的主题选择器；尺寸、字体和间距继续使用共享规则。

### 6. 接入审计

- 在 `scripts/timeline-registry.js` 注册 release、编辑解读、机制、系列影响、行业影响、剧情、决策链、里程碑和可选覆盖文件。
- 确认静态校验能够读取新的脚本标签、图片清单与本地文件。
- 为该时间线新增目录 `README.md`，记录收录范围、可见信息、文件所有权、图片原则和验证命令。

## 修改现有时间线时的边界

- 先检查工作区是否已有用户修改，不能覆盖与任务无关的改动。
- 时间线自己的 release、文本、里程碑和图片应留在自己的目录，不塞回 `common/`。
- 共享尺寸、布局、卡片结构和通用交互留在 `common/`，不要为单一系列复制共享代码。
- 修改一级卡片规范默认影响所有时间线；修改某条时间线的展开内容默认只影响该时间线。
- 新系列加入后默认进入总览筛选选项，但总览默认仍为空。
- 不要恢复已经被用户明确隐藏的主机底部旧卡片区、图片区域调试开关、研究线索或设计决策链。
- 不要把 GM 管理入口当作公开站点功能展示。

## 验证与防悬挂规则

常规验证不需要本地服务器。依次运行：

```text
node scripts/validate-markdown-docs.js
node scripts/validate-static-site.js
node scripts/audit-english-localization.js
node scripts/audit-english-terminology.js
node scripts/audit-english-prose.js
node scripts/audit-hardware-data.js
node scripts/audit-release-data.js
node scripts/audit-timeline-content.js
```

只修改游戏系列时可以不运行硬件审计；只修改硬件时仍应运行静态校验。新增时间线必须运行 release 与 content 两个审计。

禁止为了例行验证使用 `Start-Process`、`start /b`、`Start-Job` 或其他后台/隐藏服务器。只有修改 GM HTTP API 本身时才做前台、临时端口、硬超时并在 `finally` 关闭的服务测试。

## 完成任务时的交付格式

- 简要说明修改了什么以及为什么。
- 列出实际运行的验证和结果；未运行的验证要明确说明。
- 默认提供可直接粘贴到 GitHub Desktop 的 `Summary` 和 `Description`。
- 不自动提交、推送或删除用户文件，除非用户明确要求。

## 给 AI 的最后检查

在结束前逐项确认：

- 我是否读过目标时间线的收录规则，而不是凭系列名猜范围？
- 一级卡片是否继续遵守共享尺寸和区域顺序？
- 日期是否真的是全球最早公开发售日？
- 普通移植是否误建成一级卡片？
- “变化、机制、影响”是否写到了具体规则，而不是抽象套话？
- 图片是否已进入正确时间线目录和清单，并能被 GM 管理？
- 新时间线是否同步接入页面、总览、图片 store、GM 服务和审计 registry？
- 是否避免启动后台服务器？
- 文档、静态资源和专项审计是否通过？
